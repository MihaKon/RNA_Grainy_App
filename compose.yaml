services:
  app:
    build:
      context: .
    init: true
    restart: unless-stopped
    ports:
      - "127.0.0.1:5050:5050"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
